open BsReactNative;
open Style;

let flex1 = style([flex(1.)]);

let rowFlexed = style([flexDirection(Row)]);

let columnFlexed = style([flexDirection(Column)]);

let centerAligned = style([alignItems(Center)]);

let centerJustified = style([justifyContent(Center)]);